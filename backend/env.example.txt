# Server Configuration
PORT=5001
NODE_ENV=development

# Database
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_KEY=your_supabase_service_key

# JWT
JWT_SECRET=your_jwt_secret_key_here

# Apple IAP
APPLE_IAP_SHARED_SECRET=your_apple_iap_shared_secret

# Google Play IAP
GOOGLE_PLAY_SERVICE_ACCOUNT_PATH=./google-play-service-account.json
GOOGLE_PLAY_PACKAGE_NAME=com.nsyzk.lingrootmobile

# OpenAI
OPENAI_API_KEY=your_openai_api_key

# Claude AI (Anthropic)
CLAUDE_API_KEY=your_claude_api_key
# or use
ANTHROPIC_API_KEY=your_anthropic_api_key

# Google Cloud TTS
GOOGLE_APPLICATION_CREDENTIALS=./google-cloud-tts-key.json

# MFA Alignment (Montreal Forced Aligner)
# Enable MFA alignment pipeline
USE_MFA_ALIGNMENT=true
# Use remote MFA service instead of local MFA (recommended in production)
USE_REMOTE_MFA=true
# Remote MFA base URL
MFA_SERVICE_URL=https://api.booklevel.store
# Remote MFA request timeout in milliseconds (default: 300000)
MFA_REMOTE_TIMEOUT_MS=300000

# Remote MFA async mode (recommended when Cloudflare proxy causes 524 on long alignments)
MFA_REMOTE_ASYNC=true
# Poll interval for async job status (ms)
MFA_REMOTE_ASYNC_POLL_INTERVAL_MS=1500
# Overall async wait timeout (ms) (defaults to MFA_REMOTE_TIMEOUT_MS)
MFA_REMOTE_ASYNC_TIMEOUT_MS=300000

# If remote MFA fails, allow falling back to local MFA (set false to disable fallback)
MFA_REMOTE_FALLBACK_TO_LOCAL=true

# Azure Speech Services (optional)
AZURE_SPEECH_KEY=your_azure_speech_key
AZURE_SPEECH_REGION=eastus

# AWS Polly (optional)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# Email (Nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Frontend URL
FRONTEND_URL=https://lingroot.com

# Stripe (if using)
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Quality Tests
QT_API_URL=http://localhost:5001/api
QT_TEST_USER_EMAIL=test@lingroot.com
QT_TEST_USER_PASSWORD=your_test_user_password
QT_COST_LIMIT_USD=5.00
