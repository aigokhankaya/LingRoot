# QA review prompt

Review the package against its schemas, shared visual invariant, requested CEFR
levels, artifact completeness, subtitle timing, metadata requirements and
secret-leak policy.

Return explicit checks with severity, pass/fail and actionable messages.
