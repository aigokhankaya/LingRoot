# src/qa

Semantic quality assurance validates schemas, requested levels, topic/scene
references, artifact completeness and secret leakage. It emits package and
level `qa-report.json` files using `schemas/qa-report.schema.json`.
