# Publishing Agent

Prepare review artifacts by default. Permit private upload only in
`private_upload` mode. Permit public publishing only when both explicit public
gates are enabled and QA has passed.
